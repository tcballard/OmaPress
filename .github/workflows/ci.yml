name: CI
on:
  push:
    branches: [main, 'codex/**']
  pull_request:
permissions:
  contents: read
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true
jobs:
  engine:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@1.98.0
        with:
          components: rustfmt, clippy
      - run: cargo fmt --all --check
      - run: cargo clippy --workspace --all-targets --locked -- -D warnings
      - run: cargo test --workspace --locked
      - run: cargo build --workspace --locked
      - run: python3 tests/distribution.py
      - run: python3 tests/scheduling.py
      - run: node --check companion/popup.js && node --check companion/editor.js
      - run: python3 -m py_compile scripts/install-companion.py
      - run: python3 tests/deployment.py
      - run: python3 tests/preview_http.py
      - run: cargo build --release --locked
      - run: python3 scripts/benchmark.py | tee benchmark.json
      - uses: actions/upload-artifact@v4
        with:
          name: engine-benchmark
          path: benchmark.json
  native:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@1.98.0
      - run: sudo apt-get update && sudo apt-get install -y cmake qt6-base-dev qt6-declarative-dev qml6-module-qtquick qml6-module-qtquick-controls qml6-module-qtquick-layouts qml6-module-qtquick-dialogs qml6-module-qtquick-templates qml6-module-qtqml-workerscript qml6-module-qtquick-window xvfb libgl1-mesa-dri
      - run: cargo build --release --locked
      - run: cmake -S desktop -B build/desktop -DCMAKE_BUILD_TYPE=Release && cmake --build build/desktop --parallel 2
      - name: Native application and clipboard smoke
        env:
          PRESSROOM_CLI: ${{ github.workspace }}/target/release/pressroom
          QT_QUICK_BACKEND: software
        run: |
          xvfb-run -a build/desktop/pressroom-desktop --publication "$PWD/fixtures/fixing-everything" --smoke
          xvfb-run -a build/desktop/pressroom-desktop --clipboard-smoke
          xvfb-run -a build/desktop/pressroom-desktop --dialogs-smoke
      - run: python3 scripts/package.py
      - name: Install bundle and verify both binaries
        run: |
          bundle=$(find dist -mindepth 1 -maxdepth 1 -type d -name 'pressroom-*' -print -quit)
          sh "$bundle/install.sh" "$RUNNER_TEMP/pressroom-installed"
          "$RUNNER_TEMP/pressroom-installed/bin/pressroom" --version
          python3 tests/installed_companion.py "$RUNNER_TEMP/pressroom-installed"
          QT_QPA_PLATFORM=offscreen QT_QUICK_BACKEND=software "$RUNNER_TEMP/pressroom-installed/bin/pressroom-desktop" --smoke
      - uses: actions/upload-artifact@v4
        with:
          name: pressroom-linux-x86_64
          path: |
            dist/*.tar.gz
            dist/SHA256SUMS
  arch-package:
    runs-on: ubuntu-24.04
    container: archlinux:base-devel
    steps:
      - uses: actions/checkout@v4
      - name: Install build and runtime dependencies
        run: pacman -Syu --noconfirm rust cmake qt6-base qt6-declarative qt6-wayland git github-cli curl libsecret xdg-utils python desktop-file-utils namcap
      - name: Build unsigned package as an ordinary user
        run: |
          useradd --create-home builder
          chown -R builder:builder "$GITHUB_WORKSPACE"
          runuser -u builder -- bash -c 'cd "$1"; makepkg --noconfirm --cleanbuild; makepkg --printsrcinfo > actual.SRCINFO; diff -u .SRCINFO actual.SRCINFO' _ "$GITHUB_WORKSPACE/packaging/arch"
      - name: Inspect, install, launch, reinstall and remove
        shell: bash
        env:
          QT_QPA_PLATFORM: offscreen
          QT_QUICK_BACKEND: software
        run: |
          cd packaging/arch
          namcap PKGBUILD
          package=$(find . -maxdepth 1 -name 'pressroom-*.pkg.tar.zst' ! -name '*debug*' -print -quit)
          test -n "$package"
          namcap "$package" | tee namcap-report.txt
          python -c 'from pathlib import Path; assert not any(" E: " in line for line in Path("namcap-report.txt").read_text().splitlines()), "namcap reported packaging errors"'
          bsdtar -tvf "$package" > package-files.txt
          pacman -U --noconfirm "$package"
          runuser -u builder -- env QT_QPA_PLATFORM=offscreen QT_QUICK_BACKEND=software pressroom-desktop --dialogs-smoke
          runuser -u builder -- python "$GITHUB_WORKSPACE/tests/installed_companion.py" /usr
          pacman -U --noconfirm "$package"
          pacman -R --noconfirm pressroom
          test ! -e /usr/bin/pressroom
          test ! -e /usr/bin/pressroom-desktop
          test ! -e /usr/bin/pressroom-companion
      - uses: actions/upload-artifact@v4
        with:
          name: pressroom-arch-unsigned
          path: |
            packaging/arch/*.pkg.tar.zst
            packaging/arch/package-files.txt
            packaging/arch/namcap-report.txt
