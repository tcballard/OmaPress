# Install as ~/.config/omapress/worker.env, mode 0600; use absolute paths.
OMAPRESS_PUBLICATION=/srv/omapress/publication
# Optional for unattended X; omit to use an available Secret Service keyring.
OMAPRESS_X_CREDENTIALS_FILE=/srv/omapress/credentials/x.json
